{
  "name": "unginx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "sh scripts/dev.sh",
    "build": "pnpm --filter @unginx/shared build && pnpm --filter @unginx/backend build && pnpm --filter @unginx/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
